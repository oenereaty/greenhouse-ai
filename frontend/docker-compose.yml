services:
  frontend:
    build:
      context: .
      args:
        VITE_API_BASE: ${VITE_API_BASE}
        VITE_API_KEY: ${VITE_API_KEY}
    ports:
      - "${FRONTEND_PORT:-8080}:80"
    restart: unless-stopped
