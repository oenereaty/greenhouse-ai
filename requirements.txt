# Greenhouse AI backend — 전부 로컬 실행 (Ollama + BGE-M3, 클라우드 API 불필요)
# conda env: greenhouse-ai (Python 3.11)
# pip install -r requirements.txt

# RAG / vector store
chromadb>=1.5
sentence-transformers>=5.0
pypdf>=6.0
pymupdf>=1.28

# MCP server & client
mcp[cli]>=1.0

# LLM (Ollama) + KMA/KAMIS API clients
requests>=2.34

# Web UI
streamlit>=1.58

# Utilities
pyyaml>=6.0
python-dotenv>=1.0.0
apscheduler>=3.10.0
